int add(int a, int b) {
    int result = a + b;
    return result;
}

int main() {
    int x = 10;
    float y = 20.5;
    string name = "Zara";
    array arr = {1, 2, 3};
    
    if (x < y) {
        x = x + 1;
    } else {
        x = x - 1;
    }
    
    for (int i = 0; i < 10; i = i + 1) {
        x = x + i;
    }
    
    do {
        x = x + 2;
    } while (x < 20);
    
    return 0;
}
